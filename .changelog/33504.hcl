change "new-property" {
  body = "`azurerm_virtual_network_gateway_connection` - add support for the `key_vault_certificate` property"
}
change "new-property" {
  body = "`azurerm_virtual_network_gateway` - add support for the `identity` property"
}
