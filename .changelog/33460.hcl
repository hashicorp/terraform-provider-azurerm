
change "new-property" {
  body = "`azurerm_kubernetes_cluster` - add support for the `local_dns_profile` block"
}
change "new-property" {
  body = "`azurerm_kubernetes_cluster_node_pool` - add support for the `local_dns_profile` block"
}
