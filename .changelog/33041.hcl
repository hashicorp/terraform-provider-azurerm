change "new-property" {
  body = "`azurerm_monitor_data_collection_rule` - add support for the `logs_ingestion_endpoint` and `metrics_ingestion_endpoint` properties"
}
change "resource-enhancement" {
  body = "`azurerm_monitor_data_collection_rule` - support `Direct` as a value for the `kind` property"
}
