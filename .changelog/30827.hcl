
change "new-property" {
  body = "`azurerm_kubernetes_cluster` - add support for the `default_node_pool.pod_ip_allocation_mode` property"
}
change "new-property" {
  body = "`azurerm_kubernetes_cluster_node_pool` - add support for the `pod_ip_allocation_mode` property"
}
