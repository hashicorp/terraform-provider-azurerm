
change "new-property" {
  body = "`azurerm_kubernetes_cluster` - add support for the `default_node_pool.security` block"
}
change "new-property" {
  body = "`azurerm_kubernetes_cluster_node_pool` - add support for the `security` block"
}
