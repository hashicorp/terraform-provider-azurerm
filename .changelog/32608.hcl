change "breaking" {
  body = "`azurerm_kubernetes_cluster` - the `default_node_pool.upgrade_settings` block will be required in v6.0"
}
change "breaking" {
  body = "`azurerm_kubernetes_cluster_node_pool` - the `upgrade_settings` block will be required in v6.0"
}
