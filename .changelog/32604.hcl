
change "new-property" {
  body = "`azurerm_kubernetes_cluster` - add support for the `default_node_pool.message_of_the_day` property"
}
change "new-property" {
  body = "`azurerm_kubernetes_cluster_node_pool` - add support for the `message_of_the_day` property"
}
